<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://caldarius.github.io/</id>
    <title>Caldarius</title>
    <updated>2021-05-27T08:26:41.823Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://caldarius.github.io/"/>
    <link rel="self" href="https://caldarius.github.io/atom.xml"/>
    <subtitle>世界上只有一种英雄主义,就是看清生活的真相之后依然热爱生活。</subtitle>
    <logo>https://caldarius.github.io/images/avatar.png</logo>
    <icon>https://caldarius.github.io/favicon.ico</icon>
    <rights>All rights reserved 2021, Caldarius</rights>
    <entry>
        <title type="html"><![CDATA[数据库概述]]></title>
        <id>https://caldarius.github.io/post/shu-ju-ku-gai-shu/</id>
        <link href="https://caldarius.github.io/post/shu-ju-ku-gai-shu/">
        </link>
        <updated>2021-05-27T08:23:34.000Z</updated>
        <content type="html"><![CDATA[<h3 id="四个基本概念">四个基本概念</h3>
<h4 id="数据data">数据(Data)</h4>
<p>是数据库中存储的基本对象</p>
<ul>
<li>
<p>定义：描述事物的符号记录</p>
</li>
<li>
<p>数据的种类：文字、图形、图像、声音、视频等</p>
</li>
<li>
<p>数据的特点：数据与其语义是不可分的</p>
<ul>
<li>例：学生档案中的学生记录（李明,男,2001,江苏,计算机科学与技术学院,2019）</li>
</ul>
<p>数据的解释</p>
<p>语义：学生姓名、性别、出生年月、籍贯、所在学院、入学时间</p>
<p>解释：李明是个大学生，2001年出生，江苏人，2019年考入计算机科学与技术学院。</p>
</li>
</ul>
<h4 id="数据库">数据库</h4>
<ul>
<li>定义：存放数据的仓库</li>
</ul>
<p>数据库是长期储存在计算机内、有组织的、可共享的大量数据集合</p>
<ul>
<li>
<p>数据库的特征：</p>
<ul>
<li>
<p>数据按一定的数据模型组织、描述和储存**</p>
</li>
<li>
<p>可为各种用户共享</p>
</li>
<li>
<p>冗余度较小</p>
</li>
<li>
<p>数据独立性较高</p>
</li>
<li>
<p>易扩展</p>
</li>
</ul>
</li>
</ul>
<h4 id="dbms数据库管理系统">DBMS(数据库管理系统)</h4>
<ul>
<li>
<p>定义：数据库管理系统是位于用户与操作系统之间的一层数据管理软件。</p>
</li>
<li>
<p>用途：科学地组织和存储数据、高效地获取和维护数据。</p>
</li>
<li>
<p>的主要功能：</p>
<ul>
<li>
<p>数据定义功能：提供数据定义语言(DDL)</p>
</li>
<li>
<p>数据组织存储管理</p>
</li>
<li>
<p>数据操纵功能：提供数据操纵语言(DML)</p>
</li>
<li>
<p>数据库的事务管理和运行管理</p>
</li>
<li>
<p>数据库的建立和维护功能(实用程序)</p>
</li>
<li>
<p>Others</p>
</li>
</ul>
</li>
</ul>
<h4 id="dbs数据库系统">DBS(数据库系统)</h4>
<ul>
<li>定义：数据库系统(Database System，简称DBS)是指在计算机系统中引入数据库后的系统构成。在不引起混淆的情况下常常把数据库系统简称为数据库</li>
<li>数据库系统的构成：由数据库、数据库管理系统（及其开发工具）、应用系统、数据库管理员（和用户）构成。</li>
<li>数据管理：对数据进行分类、组织、编码、存储、检索和维护，是数据处理的中心问题。</li>
<li>数据管理技术的发展过程</li>
</ul>
<table>
<thead>
<tr>
<th>阶段</th>
<th>时间</th>
</tr>
</thead>
<tbody>
<tr>
<td>人工管理阶段</td>
<td>40年代中--50年代中</td>
</tr>
<tr>
<td>文件系统阶段</td>
<td>50年代末--60年代中</td>
</tr>
<tr>
<td>数据库系统阶段</td>
<td>60年代末--现在</td>
</tr>
</tbody>
</table>
<ul>
<li>
<p>数据管理技术的发展动力</p>
<ul>
<li>
<p>应用需求的推动</p>
</li>
<li>
<p>计算机硬件的发展</p>
</li>
<li>
<p>计算机软件的发展</p>
</li>
</ul>
</li>
</ul>
<h5 id="数据库系统特点">数据库系统特点：</h5>
<ul>
<li>
<p>数据结构化</p>
<ul>
<li>
<p>整体数据的结构化是数据库的主要特征之一。</p>
</li>
<li>
<p>数据库中实现的是数据的真正结构化</p>
</li>
<li>
<p>数据的结构用数据模型描述，无需程序定义和解释。</p>
</li>
<li>
<p>数据可以变长。</p>
</li>
<li>
<p>数据的最小存取单位是数据项。</p>
</li>
</ul>
</li>
<li>
<p>数据的共享性高、冗余度低、易扩充</p>
<ul>
<li>
<p>降低数据的冗余度，节省存储空间</p>
</li>
<li>
<p>避免数据间的不一致性</p>
</li>
<li>
<p>使系统易于扩充</p>
</li>
</ul>
</li>
<li>
<p>数据独立性高</p>
<ul>
<li>
<p>物理独立性：指用户的应用程序与存储在磁盘上的数据库中数据是相互独立的。当数据的物理存储改变了，应用程序不用改变。</p>
</li>
<li>
<p>逻辑独立性：指用户的应用程序与数据库的逻辑结构是相互独立的。数据的逻辑结构改变了，用户程序也可以不变。</p>
</li>
</ul>
</li>
</ul>
<h5 id="dbms对数据的控制功能">DBMS对数据的控制功能</h5>
<ul>
<li>
<p>数据的安全性（Security）保护</p>
<p>使每个用户只能按指定方式使用和处理指定数据，保护数据以防止不合法的使用造成的数据的泄密和破坏。**</p>
</li>
<li>
<p>数据的完整性（Integrity）检查</p>
<p>将数据控制在有效的范围内，或保证数据之间满足一定的关系。</p>
</li>
<li>
<p>并发（Concurrency）控制</p>
<p>对多用户的并发操作加以控制和协调，防止相互干扰而得到错误的结果。</p>
</li>
<li>
<p>数据库恢复（Recovery）</p>
<p>将数据库从错误状态恢复到某一已知的正确状态。</p>
</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[数据库完整性]]></title>
        <id>https://caldarius.github.io/post/shu-ju-ku-wan-zheng-xing/</id>
        <link href="https://caldarius.github.io/post/shu-ju-ku-wan-zheng-xing/">
        </link>
        <updated>2021-05-21T12:16:03.000Z</updated>
        <content type="html"><![CDATA[<h1 id="数据库完整性">数据库完整性</h1>
<p><strong>数据库完整性确保的是数据的正确性和相容性</strong></p>
<ul>
<li>防止数据库中存在不符合语义的数据，也就是防止数据库中存在不正确的数据。</li>
<li>防范的对象：不和语义的、不正确的 <strong>数据</strong></li>
<li>DBMS要做的
<ul>
<li>提供定义 完整性约束条件 的机制。</li>
<li>提供完整性检查的方法。</li>
<li>违约处理机制。</li>
</ul>
</li>
</ul>
<h2 id="实体完整性">实体完整性</h2>
<h3 id="定义">定义</h3>
<ul>
<li>
<p>若属性A（一个或一组属性）是基本关系R的主属性，则A不能取空值。</p>
<p>所谓空值就是“不知道”或“不存在”或“无意义”的值。</p>
</li>
<li>
<p>实体完整性规则是针对基本关系而言的。</p>
</li>
<li>
<p>现实世界的实体是可以区分的，他们具有某种唯一性标识。</p>
</li>
<li>
<p>相应地，关系模型中以主码作为唯一性标识。</p>
</li>
<li>
<p>主码中的属性不能取空值。如果主属性取空值，就说明存在某个不可比标识的实体，与1.2相违背，因此这个规则称为实体完整性。</p>
</li>
</ul>
<pre><code class="language-mysql">create table `student`(
	Sno char(11) not null,
    Cno char(11) not null,
	Sname varchar(10)notnull,
    primary key(Sno,Cno)
); -- 表级的主键定义
create table `Course`(
	Cno char(11) not null primarykey,
    Cname varchar(20)
); -- 列级定义主键

</code></pre>
<h3 id="检查和违约处理">检查和违约处理</h3>
<ul>
<li>检查主码值是否唯一，如果不唯一则拒绝插入或修改</li>
<li>检查主码的各个属性是否为空，只要有一个为空就拒绝插入或修改</li>
</ul>
<h2 id="参照完整性">参照完整性</h2>
<h3 id="定义-2">定义</h3>
<ul>
<li>现实世界中的实体之间往往存在着某种联系，在关系模型中，实体以及实体之间的联系都是用关系来表示的，这样就自然存在着关系与关系之间的引用。</li>
<li>参照完整性用于描述实体之间的联系。</li>
<li>参照完整性一般是指多个实体或关系之间的关联关系。</li>
<li>参照完整性规则就是定义外键与被参照的主键之间的引用规则。</li>
<li>外键要么值为空要么等于其所参照的关系中的某个元组的主键值。</li>
</ul>
<p><strong>外键会拖垮查询效率，用程序级别实现</strong></p>
<pre><code class="language-mysql">create table `student`(
	Sno char(11) not null,
    Cno char(11) not null,
	Sname varchar(10)notnull,
    primary key(Sno,Cno),
    foreign key(Cno)references Course(Cno) -- 表级定义的参照完整性
);
</code></pre>
<table>
<thead>
<tr>
<th>被参照表</th>
<th>参照表</th>
<th>违约处理</th>
</tr>
</thead>
<tbody>
<tr>
<td>可能破坏参照完整性</td>
<td>插入元组</td>
<td>拒绝</td>
</tr>
<tr>
<td>可能破坏参照完整性</td>
<td>修改外键值</td>
<td>拒绝</td>
</tr>
<tr>
<td>删除元组</td>
<td>可能破坏参照完整性</td>
<td>拒绝/级联修改/设为控制</td>
</tr>
<tr>
<td>修改主键值</td>
<td>可能破坏参照完整性</td>
<td>拒绝/级联修改/设为控制</td>
</tr>
</tbody>
</table>
<pre><code class="language-mysql">CREATE TABLE SC
        (Sno   CHAR(9)  NOT NULL，
         Cno   CHAR(4)  NOT NULL，
         Grade  SMALLINT，
         PRIMARY KEY（Sno，Cno）， 				
         FOREIGN KEY (Sno) REFERENCES Student(Sno) 
		ON DELETE CASCADE     /*级联删除SC表中相应的元组*/
                ON UPDATE CASCADE， /*级联更新SC表中相应的元组*/
         FOREIGN KEY (Cno) REFERENCES Course(Cno) 	                    
               ON DELETE NO ACTION 	
               	/*当删除course 表中的元组造成了与SC表不一致时拒绝删除*/
               ON UPDATE CASCADE   
      			/*当更新course表中的cno时，级联更新SC表中相应的元组*/
        )；
</code></pre>
<h3 id="用户定义的完整性">用户定义的完整性</h3>
<p>**针对某一具体应用的数据必须满足的语义要求 **</p>
<ul>
<li>是针对语义定义的数据约束条件。</li>
<li>实际上就是指明关系中属性的取值范围，防止属性的值与语义矛盾</li>
<li>关系模型应提供定义和检验这类完整性的机制，以便用统一的系统方法处理它们，而不要由应用程序承担这一功能。</li>
</ul>
<h4 id="定义-3">定义</h4>
<ul>
<li>建表时
<ul>
<li>not null 列值非空。</li>
<li>unique 列值唯一。</li>
<li>check（ ）检查列值是否满足一个布尔表达式。</li>
</ul>
</li>
</ul>
<h4 id="违约处理">违约处理</h4>
<ul>
<li>插入元组或者修改属性值时，DBMS检查属性上的约束条件是否被满足，如果不满足则拒绝执行</li>
</ul>
<h3 id="完整性约束命名子句">完整性约束命名子句</h3>
<ul>
<li>
<p>constaint约束</p>
<pre><code class="language-mysql">constraint &lt;Cname&gt;
[primary key...
 foreign key...
 check...]
 
 CREATE TABLE Student(
     	Sno  NUMERIC(6)
        CONSTRAINT C1 CHECK (Sno BETWEEN 90000 AND 99999)，
        Sname  CHAR(20)  
        CONSTRAINT C2 NOT NULL，
        Sage  NUMERIC(3)
        CONSTRAINT C3 CHECK (Sage &lt; 30)，
        Ssex  CHAR(2)
        CONSTRAINT C4 CHECK (Ssex IN ( '男'，'女'))，
        CONSTRAINT StudentKey PRIMARY KEY(Sno)
 )；
-- -----------修改约束条件
ALTER TABLE Student
        DROP CONSTRAINT C1;
        ALTER TABLE Student
        ADD CONSTRAINT C1 CHECK (Sno BETWEEN 900000 AND 999999)，
        ALTER TABLE Student
        DROP CONSTRAINT C3;
        ALTER TABLE Student
        ADD CONSTRAINT C3 CHECK (Sage &lt; 40)；
</code></pre>
</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[数据库安全性]]></title>
        <id>https://caldarius.github.io/post/shu-ju-ku-an-quan-xing/</id>
        <link href="https://caldarius.github.io/post/shu-ju-ku-an-quan-xing/">
        </link>
        <updated>2021-05-21T12:15:04.000Z</updated>
        <content type="html"><![CDATA[<h1 id="数据库安全性">数据库安全性</h1>
<h2 id="数据库安全性-2">数据库安全性</h2>
<h3 id="问题">问题</h3>
<ol>
<li>数据库的特点之一是数据共享，但是数据共享必然带来安全性问题。</li>
<li>数据库系统中的数据共享不能是无条件的共享。</li>
</ol>
<h3 id="数据库安全性控制">数据库安全性控制</h3>
<h4 id="用户表示与鉴别">用户表示与鉴别</h4>
<p><strong>系统提供的最外层的安全保护措施</strong>。</p>
<ul>
<li>
<p>用户标识</p>
<p>系统核对口令以鉴别用户身份。为了防止口令的窃取会有各种防御措施来预防。典型的有生物识别，口令计算函数。</p>
</li>
</ul>
<h4 id="存取控制">存取控制</h4>
<p><strong>作用在DBMS级别的安全措施</strong></p>
<ul>
<li>定义用户权限</li>
<li>合法权限的检查</li>
</ul>
<h5 id="实现方法">实现方法</h5>
<ul>
<li>通过SQL 的 <strong>GRANT REVOKE</strong> 语句实现。</li>
<li>用户权限组成：
<ul>
<li>数据对象</li>
<li>操作类型</li>
</ul>
</li>
<li>授权：定义用户可以在哪些数据库对象上镜像哪些类型的操作</li>
</ul>
<table>
<thead>
<tr>
<th>对象</th>
<th>操作类型</th>
</tr>
</thead>
<tbody>
<tr>
<td>模式（schema）</td>
<td>CREATE SCHEMA</td>
</tr>
<tr>
<td>基本表（table）</td>
<td>CREATE TABLE，ALTER TABLE</td>
</tr>
<tr>
<td>视图（view）</td>
<td>CREATE VIEW</td>
</tr>
<tr>
<td>索引（index'）</td>
<td>CREATE INDEX</td>
</tr>
<tr>
<td>数据（基本表和视图）</td>
<td>SELECT，INSERT，UPDATE，DELETE，REFERENCES，ALL PRIVILEGES</td>
</tr>
<tr>
<td>属性列</td>
<td>SELECT，INSERT，UPDATE， REFERENCES，ALL PRIVILEGES</td>
</tr>
</tbody>
</table>
<h3 id="操作语句">操作语句</h3>
<h4 id="grant">GRANT</h4>
<p>GRANT &lt;权限&gt;[,&lt;权限&gt;]...</p>
<p>​    [ON &lt;对象类型&gt; &lt;对象名&gt;]</p>
<p>​    TO &lt;用户&gt;[,&lt;用户&gt;]...</p>
<p>​    [WITH GRANT OPTION];</p>
<h5 id="授权">授权</h5>
<ul>
<li>DBA。</li>
<li>是数据库的创建者。</li>
<li>拥有权限的用户。</li>
</ul>
<h5 id="接受">接受</h5>
<ul>
<li>一个或者多个具体的用户。</li>
<li>PUBLIC  （全体用户）。</li>
</ul>
<h5 id="with-grant-option">WITH GRANT OPTION;</h5>
<ul>
<li>指定，可以再授予。</li>
<li>没有，不能传播。</li>
<li>不允许循环授权，A-&gt;B-&gt;C-&gt;A</li>
</ul>
<pre><code class="language-mysql">grant select,insert
	on table `student`,`grade`
	to `t1`,`t2`;
	
grant all priviliges
	on table student,course
	to t1,t2;
	
grant select
	on table student,grade
	to public;
	
grant all priviliges
	on table student,course
	to t1,t2
	with grant option;
</code></pre>
<h4 id="revoke">REVOKE</h4>
<p><strong>除  TO-&gt;FROM   基本语法格式与 GRANT 语句相同，不再赘述</strong></p>
<ul>
<li>对于权限传播的用户权限收回在FROM语句之后+“CASCADE”即可。</li>
</ul>
<pre><code class="language-mysql">revoke all priviliges
	on table student
	from t1
	cascade
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[关系数据库理论（二）]]></title>
        <id>https://caldarius.github.io/post/guan-xi-shu-ju-ku-li-lun-er/</id>
        <link href="https://caldarius.github.io/post/guan-xi-shu-ju-ku-li-lun-er/">
        </link>
        <updated>2021-05-21T12:13:38.000Z</updated>
        <content type="html"><![CDATA[<h1 id="关系数据库理论二">关系数据库理论（二）</h1>
<h2 id="范式">范式</h2>
<h3 id="定义">定义</h3>
<ul>
<li>范式是符合某一种级别的关系模式的集合。定义的是规范。</li>
</ul>
<h3 id="种类">种类</h3>
<ul>
<li>第一范式 (1NF)</li>
<li>第二范式 (2NF)</li>
<li>第三范式 (3NF)</li>
<li>BC范式 (BCNF)</li>
<li>……</li>
</ul>
<h3 id="联系">联系</h3>
<p>低级别的范式包含高级别的范式~</p>
<h3 id="规范化">规范化</h3>
<p>一个低级别范式的关系模式，通过模式分解转化为若干高一级范式的关系模式的集合 的过程。</p>
<h3 id="第一范式-1nf">第一范式 (1NF)</h3>
<h4 id="定义-2">定义</h4>
<p>如果一个关系模式R的所有属性都是不可分的 基本数据项，则R∈1NF。</p>
<h4 id="意义">意义</h4>
<ul>
<li>第一范式是对关系模式的最起码的要求。不满 足第一范式的数据库模式不能称为关系数据库。</li>
<li>但是满足第一范式的关系模式并不一定是一个 好的关系模式。我们需要更细致的规范。</li>
</ul>
<h3 id="第二范式-2nf">第二范式 (2NF)</h3>
<h4 id="引例">引例</h4>
<p>关系模式 SLC(Sno, Sdept, Sloc, Cno, Grade) ，Sloc为学生住处，假设每个系的学生住在同一 个地方。</p>
<p>FD（函数依赖）：</p>
<p>​	(Sno, Cno) f-&gt;Grade</p>
<p>​	 Sno -&gt; Sdept</p>
<p>​	(Sno, Cno) -&gt; Sdept</p>
<p>​	Sno -&gt; Sloc</p>
<p>​	(Sno, Cno) -&gt; Sloc</p>
<p>​	Sdept -&gt; Sloc</p>
<p><strong>SLC：</strong></p>
<table>
<thead>
<tr>
<th>Sno</th>
<th>Sdept</th>
<th>Sloc</th>
<th>Cno</th>
<th>Grade</th>
</tr>
</thead>
<tbody>
<tr>
<td></td>
<td></td>
<td></td>
<td></td>
<td></td>
</tr>
</tbody>
</table>
<h5 id="分析">分析</h5>
<ul>
<li>
<p>SLC的码为(Sno, Cno)。</p>
</li>
<li>
<p>非主属性Sdept和Sloc部分函数依赖于码(Sno, Cno)</p>
</li>
<li>
<p>插入异常</p>
<p>假设Sno＝0121，Sdept＝IS，Sloc＝N 的学生还未选课，因课程号是主 属性，因此该学生的信息无法插入SLC。</p>
</li>
<li>
<p>删除异常</p>
<p>假定某个学生本来只选修了3号课程这一门课。现在因身体不适，他连3 号课程也不选修了。因课程号是主属性，此操作将导致该学生信息的 整个元组都要删除。</p>
</li>
<li>
<p>数据冗余度大</p>
<p>如果一个学生选修了10门课程，那么他的Sdept和Sloc值就要重复 存储了10次。</p>
</li>
<li>
<p>修改复杂</p>
<p>如学生转系，在修改此学生元组的Sdept值的同时，还可能需 要修改住处（Sloc）。如果这个学生选修了K门课，则必须无遗 漏地修改K个元组中全部Sdept、Sloc信息。</p>
</li>
<li>
<p>原因</p>
<p>Sdept、 Sloc部分函数依赖于码。</p>
</li>
<li>
<p>解决方法</p>
<p>SLC分解为两个关系模式，以消除这些部分函数依赖。（ 关系模式分解将在（四）中详细写明 ）</p>
<p>SC（Sno， Cno， Grade）</p>
<p>SL（Sno， Sdept， Sloc）</p>
</li>
</ul>
<h4 id="定义-3">定义</h4>
<ul>
<li>若关系模式R∈1NF，并且每一个非主属性都 完全函数依赖于R的码，则R∈2NF。</li>
</ul>
<h4 id="作用及缺陷">作用及缺陷</h4>
<ul>
<li>采用投影分解法将一个1NF的关系分解为多个2NF的关 系，可以在一定程度上减轻原1NF关系中存在的插入 异常、删除异常、数据冗余度大、修改复杂等问题。</li>
<li>将一个1NF关系分解为多个2NF的关系，并不能完全消 除关系模式中的各种异常情况和数据冗余。</li>
</ul>
<h3 id="3nf">3NF</h3>
<h4 id="引例-2">引例</h4>
<ul>
<li>
<p>2NF关系模式SL(Sno, Sdept, Sloc)中：</p>
<ul>
<li>
<p>函数依赖：</p>
<p>Sno -&gt; Sdept</p>
<p>Sdept -&gt; Sloc</p>
<p>Sloc 传递函数依赖于 Sloc 。</p>
</li>
</ul>
</li>
<li>
<p>解决方法：</p>
<ul>
<li>
<p>采用投影分解法，把SL分解为两个关系模式，以消除传递函数 依赖：</p>
<p>SD（Sno， Sdept）</p>
<p>DL（Sdept， Sloc）</p>
</li>
</ul>
</li>
</ul>
<h4 id="定义-4">定义</h4>
<ul>
<li>
<p>关系模式R 中若不存在这样的码X、 属性组Y及非主属性Z（Z 不包含于 Y）, 使得X-&gt; Y，Y -&gt; Z，成 立，    Y -&gt; X，则称R ∈ 3NF。</p>
</li>
<li>
<p>即：若R∈3NF，则R的每一个非主属性既不部分函数依赖于 候选码也不传递函数依赖于候选码。</p>
<h4 id="作用及缺陷-2">作用及缺陷</h4>
<p>将一个2NF关系分解为多个3NF的关系后，并不能完全消 除关系模式中的各种异常情况和数据冗余。</p>
</li>
</ul>
<h3 id="bcnf">BCNF</h3>
<h4 id="定义-5">定义</h4>
<ul>
<li>
<p>设关系模式R∈1NF，如果对于R的每个 函数依赖X -&gt; Y，若Y不属于X，则X必含有候选码，那么 R∈BCNF。</p>
</li>
<li>
<p>即：若R∈BCNF</p>
<ul>
<li>每一个决定属性集（因素）都包含（候选）码。</li>
<li>R中的所有属性（主，非主属性）都完全函数依赖于码。</li>
<li>R∈3NF.</li>
</ul>
</li>
</ul>
<h4 id="性质">性质</h4>
<ul>
<li>所有非主属性都完全函数依赖于每个候选码。</li>
<li>所有主属性都完全函数依赖于每个不包含它的 候选码。</li>
<li>没有任何属性完全函数依赖于非码的任何一组 属性</li>
</ul>
<h4 id="例子">例子</h4>
<p>在关系模式STJ（S，T，J）中，S表示学生，T表示教 师，J表示课程。</p>
<p>(S，J) -&gt; T，(S，T) -&gt; J，T -&gt; J</p>
<p>STJ∈3NF (S，J)和(S，T)都可以作为候选码 S、T、J都是主属性</p>
<p>STJ不属于BCNF T -&gt; J，T是决定属性集，T不是候选码</p>
<p><strong>解决方法：将STJ分解为二个关系模式：</strong></p>
<p>​	 <strong>ST(S，T) ∈ BCNF， TJ(T，J)∈ BCNF</strong></p>
<h3 id="多值依赖">多值依赖</h3>
<p>PS：多值依赖以及第四范式笔者正在寻找更恰当的描述方式，找到之后会第一时间更新。</p>
<h3 id="第四范式">第四范式</h3>
<h3 id="规范化基本步骤">规范化基本步骤</h3>
<ol>
<li>1NF  消除非主属性对码的部分函数依赖。</li>
</ol>
<h4 id="消除决定属性集-非码-的非平凡函数依赖">消除决定属性集 非码 的非平凡函数依赖</h4>
<ol start="2">
<li>2NF 消除非主属性对码的传递函数依赖。</li>
<li>3NF 消除主属性对码的部分和传递函数依赖。</li>
<li>BCNF 消除非平凡且非函数依赖的多值依赖。</li>
</ol>
<h4 id="基本思想">基本思想</h4>
<ul>
<li>消除不合适的数据依赖的各关系模式达到某种程度的“分 离”。</li>
<li>采用“一事一地”的模式设计原则。</li>
<li>所谓规范化实质上是概念的单一化。</li>
<li>规范化程度高不等于关系模式好，在实际应用中我们常常会在实际需求与规范化中做 trade off。笔者认为，这个妥协的过程广泛地存在于工程应用中。</li>
<li>在设计数据库模式结构时，必须对现实世界的实际情况和用 户应用需求作进一步分析，确定一个合适的、能够反映现实 世界的模式。</li>
<li>规范化过程可以在其中任何一步终止。</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[关系数据库理论（一）]]></title>
        <id>https://caldarius.github.io/post/guan-xi-shu-ju-ku-li-lun-yi/</id>
        <link href="https://caldarius.github.io/post/guan-xi-shu-ju-ku-li-lun-yi/">
        </link>
        <updated>2021-05-21T12:12:12.000Z</updated>
        <content type="html"><![CDATA[<h1 id="关系数据库理论一">关系数据库理论（一）</h1>
<h2 id="一些基本概念">一些基本概念</h2>
<h3 id="关系">关系</h3>
<ul>
<li>
<p>用于描述实体、属性、实体之间的联系。</p>
</li>
<li>
<p>从形式上看，它是一张二维表，是所涉及属性的笛卡尔积的一 个子集。</p>
</li>
</ul>
<h3 id="关系模式">关系模式</h3>
<ul>
<li>用来定义关系。</li>
<li><strong>关系数据库中的关系模式是型，而关系是值。关系模式是对关系的描写叙述。</strong></li>
</ul>
<h3 id="关系数据库">关系数据库</h3>
<ul>
<li>
<p>基于关系模型的数据库，利用关系来描述现实世界。</p>
</li>
<li>
<p>从形式上看，它由一组关系组成。（多张表组成一个数据库嘛~）</p>
</li>
</ul>
<h3 id="关系数据库的模式">关系数据库的模式</h3>
<ul>
<li>定义这组关系的关系模式的全体。</li>
</ul>
<h4 id="形式化定义">形式化定义</h4>
<p>R（U，D，DOM，F）</p>
<table>
<thead>
<tr>
<th>字符</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td>R</td>
<td>关系名,是符号化的元组语义</td>
</tr>
<tr>
<td>U</td>
<td>组成该关系的属性名的集合,一组属性</td>
</tr>
<tr>
<td>D</td>
<td>属性组U中属性所来自的域</td>
</tr>
<tr>
<td>DOM</td>
<td>属性向域的映象集合</td>
</tr>
<tr>
<td>F</td>
<td>属性间数据的依赖关系集合（关系内部属性与属性之间的一种约束关<strong>系</strong>，主要分函数依赖和多值依赖）</td>
</tr>
</tbody>
</table>
<p>由于D、DOM与模式设计关系不大，所以常常把关系模式看作一个三元组：<strong>R&lt;U,F&gt;</strong>。</p>
<p><strong>当且仅当U上的一个关系r 满足F时，r称为关 系模式 R（U, F）的一个关系。</strong></p>
<h3 id="数据依赖">数据依赖</h3>
<h4 id="完整性约束的表现形式">完整性约束的表现形式</h4>
<ul>
<li>限定属性取值范围：例如学生成绩必须在0-100之间。</li>
<li>定义属性值间的相互关连（主要体现于值的相等与否），这就是 数据依赖，它是数据库模式设计的关键。</li>
</ul>
<h4 id="数据依赖-2">数据依赖</h4>
<ul>
<li>是通过一个关系中属性间值的相等与否体现出来的数据间的相互关系。</li>
<li>是现实世界属性间相互联系的抽象，是数据内在的性质 ，是语义的体现。 （一种 A-&gt;B的感觉）</li>
<li>数据依赖的类型：
<ul>
<li>函数依赖（Functional Dependency，简记为FD）。</li>
<li>多值依赖（Multivalued Dependency，简记为MVD）。</li>
</ul>
</li>
</ul>
<h2 id="问题的提出">问题的提出</h2>
<ul>
<li>针对具体的问题，如何构造一个适用于它的数据模式。</li>
<li>如何进行数据库的逻辑设计？ -&gt;关系数据库的规范化理论。</li>
</ul>
<h3 id="示例">示例</h3>
<h4 id="数据库描述">数据库描述</h4>
<p>学生的学号（Sno）、所在系（Sdept）、系主任姓名 （Mname）、课程名（Cname）、 成绩（Grade）</p>
<p>单一的关系模式 ： Student&lt;U,F&gt;</p>
<p>U ＝｛ Sno, Sdept, Mname, Cname, Grade ｝</p>
<table>
<thead>
<tr>
<th>Sno</th>
<th>Cname</th>
<th>Grade</th>
<th>Sdept</th>
<th>name</th>
</tr>
</thead>
<tbody>
<tr>
<td></td>
<td></td>
<td></td>
<td></td>
<td></td>
</tr>
</tbody>
</table>
<h4 id="问题">问题</h4>
<ul>
<li>巨大的数据冗余。系主任系号的名字反复出现。</li>
<li>更新异常。更新数据时，维护数据完整性代价大。</li>
<li>插入异常。例，如果一个系刚成立，尚无学生，我们就无法把这个系及其系主 任的信息存入数据库。</li>
<li>删除异常。如果某个系的学生全部毕业了， 我们在删除该系学生信息的 同时，把这个系及其系主任的信息也丢掉了。</li>
</ul>
<h4 id="原因">原因</h4>
<ul>
<li>由于模式中存在某些数据以来</li>
</ul>
<h4 id="解决">解决</h4>
<ul>
<li>通过分解关系模式来消除其中不合适的数据依赖。</li>
</ul>
<h2 id="规范化">规范化</h2>
<h3 id="函数依赖">函数依赖</h3>
<h4 id="定义">定义</h4>
<p>设R(U)是一个属性集U上的关系模式，X和Y是U的子 集。若对于R(U)的任意一个可能的关系r，r中不可能存在两 个元组在X上的属性值相等， 而在Y上的属性值不等， 则 称 “X函数确定Y” 或 “Y函数依赖于X”，记作X→Y。 X称为这个函数依赖的决定属性集(Determinant)。 Y=f(x)</p>
<p><strong>x可唯一确定y</strong></p>
<h4 id="说明">说明</h4>
<ul>
<li>R的所有关系实例均要满足的约束条件。</li>
<li>函数依赖是语义范畴的概念。只能根据数据的语义来确定函数依赖。 例如“姓名→年龄”这个函数依赖只有在不允许有同名人的条件下成立。</li>
<li>数据库设计者可以对现实世界作强制的规定。例如规定不允许同名人 出现，函数依赖“姓名→年龄”成立。所插入的元组必须满足规定 的函数依赖，若发现有同名人存在， 则拒绝装入该元组。</li>
</ul>
<h3 id="平凡函数依赖与非平凡函数依赖">平凡函数依赖与非平凡函数依赖</h3>
<p>在关系模式R(U)中，对于U的子集X和Y:</p>
<p>​	如果X-&gt;Y，但Y 不包含于 X，则称X-&gt;Y是非平凡的函数依赖</p>
<p>​</p>
<p>​	若X→Y，但Y 包含于X, 则称X→Y是平凡的函数依赖</p>
<p>在关系SC(Sno, Cno, Grade)中：</p>
<table>
<thead>
<tr>
<th>类型</th>
<th>示例</th>
</tr>
</thead>
<tbody>
<tr>
<td>非平凡函数依赖</td>
<td>(Sno, Cno) → Grade</td>
</tr>
<tr>
<td>平凡函数依赖</td>
<td>(Sno, Cno) → Sno</td>
</tr>
<tr>
<td>平凡函数依赖</td>
<td>(Sno, Cno) → Cno</td>
</tr>
</tbody>
</table>
<p>易知：对于任一关系模式，平凡函数依赖都是必然成立的，它不反映新的 语义。所以我们默			认讨论非平凡函数依赖。</p>
<h3 id="完全函数依赖于部分函数依赖">完全函数依赖于部分函数依赖</h3>
<h4 id="定义-2">定义</h4>
<p>在关系模式R(U)中，如果X -&gt; Y，并且对 于X的任何一个真子集X’，都有 X’ !-&gt; Y, 则称Y完全函数依赖于X。 若X→Y，但Y不完全函数依赖于X，则称Y部分 函数依赖于X。</p>
<h4 id="示例-2">示例</h4>
<p>在关系SC(Sno, Cno, Grade)中，：</p>
<p>由于：Sno -&gt;Grade，Cno -&gt; Grade</p>
<p>因此：(Sno, Cno) 函数确定 Grade，Grade 函数依赖于(Sno, Cno)</p>
<h3 id="传递函数依赖">传递函数依赖</h3>
<h4 id="定义-3">定义</h4>
<p>在关系模式R(U)中，如果X-&gt;Y，Y-&gt;Z， 且Y !-&gt; X，Y-&gt;X，则称Z传递函数依赖于X。</p>
<p>ps :  如果Y-&gt;X， 即X&lt;--&gt;Y，则Z直接依赖于X。可以双向的传递就不是传递函数依赖了。</p>
<h4 id="示例-3">示例</h4>
<p>在关系Std(Sno, Sdept, Mname)中：</p>
<p>有： Sno -&gt; Sdept，Sdept -&gt; Mname</p>
<p>​	Mname传递函数依赖于Sno。</p>
<h2 id="码">码🐎</h2>
<h3 id="定义-4">定义</h3>
<p>设K为关系模式R中的属性或属性组合。若 K 函数确定 U，则K称为R的一个侯选码（Candidate Key）。 若关系模式R有多个候选码，则选定其中的一个做为主 码（Primary key）。</p>
<h2 id="外码">外码</h2>
<h3 id="定义-5">定义</h3>
<p>关系模式 R 中属性或属性组 X 并非 R  的码，但 X 是另一个关系模式的码，则称 X  是R 的外部码（Foreign key）也称外码 主码又和外部码一起提供了表示关系间联系的手段。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[二进制记事]]></title>
        <id>https://caldarius.github.io/post/er-jin-zhi-ji-shi/</id>
        <link href="https://caldarius.github.io/post/er-jin-zhi-ji-shi/">
        </link>
        <updated>2021-05-09T12:49:47.000Z</updated>
        <content type="html"><![CDATA[<h2 id="计算机与二进制">计算机与二进制</h2>
<p>在研究中……~</p>
<h2 id="简述">简述</h2>
<ul>
<li>采用了0、1两个基本数字符号。</li>
<li>进位规律——“逢二进一”</li>
<li>位权——“2的整数次幂”</li>
</ul>
<h3 id="优点">优点</h3>
<ul>
<li>运算简单、物理实现容易、存储和传送方便可靠。</li>
<li>在数字系统中普遍存在。</li>
</ul>
<h3 id="缺陷">缺陷</h3>
<ul>
<li>数字位数太长，字符单调，书写记忆以和阅读不方便。所以常常采用八进制或者十六进制来作为二进制数的缩写。</li>
</ul>
<h3 id="十六-十-八-二-对照表">十六、十、八、二、对照表</h3>
<table>
<thead>
<tr>
<th>十进制</th>
<th>二进制</th>
<th>八进制</th>
<th>十六进制</th>
</tr>
</thead>
<tbody>
<tr>
<td>0</td>
<td>0000</td>
<td>00</td>
<td>0</td>
</tr>
<tr>
<td>1</td>
<td>0001</td>
<td>01</td>
<td>1</td>
</tr>
<tr>
<td>2</td>
<td>0010</td>
<td>02</td>
<td>2</td>
</tr>
<tr>
<td>3</td>
<td>0011</td>
<td>03</td>
<td>3</td>
</tr>
<tr>
<td>4</td>
<td>0100</td>
<td>04</td>
<td>4</td>
</tr>
<tr>
<td>5</td>
<td>0101</td>
<td>05</td>
<td>5</td>
</tr>
<tr>
<td>6</td>
<td>0110</td>
<td>06</td>
<td>6</td>
</tr>
<tr>
<td>7</td>
<td>0111</td>
<td>07</td>
<td>7</td>
</tr>
<tr>
<td>8</td>
<td>1000</td>
<td>10</td>
<td>8</td>
</tr>
<tr>
<td>9</td>
<td>1001</td>
<td>11</td>
<td>9</td>
</tr>
<tr>
<td>10</td>
<td>1010</td>
<td>12</td>
<td>A</td>
</tr>
<tr>
<td>11</td>
<td>1011</td>
<td>13</td>
<td>B</td>
</tr>
<tr>
<td>12</td>
<td>1100</td>
<td>14</td>
<td>C</td>
</tr>
<tr>
<td>13</td>
<td>1101</td>
<td>15</td>
<td>D</td>
</tr>
<tr>
<td>14</td>
<td>1110</td>
<td>16</td>
<td>E</td>
</tr>
<tr>
<td>15</td>
<td>111</td>
<td>17</td>
<td>F</td>
</tr>
</tbody>
</table>
<h3 id="数值转换">数值转换</h3>
<h4 id="多项式替代法">多项式替代法</h4>
<ul>
<li>将二进制表示数按位权展开，并运用十进制运算法则计算，所得结果即为对应十进制数。可推广到n进制转m进制。</li>
</ul>
<h3 id="基数乘除法">基数乘除法</h3>
<ul>
<li>
<p>整数转换——“除二取余”。</p>
</li>
<li>
<p>小数转换——“乘二取整”</p>
<ul>
<li>
<p>例如：</p>
<p>0.625 * 2 = 1.250</p>
<p>0.250*2 = 0.500</p>
<p>0.500*2 = 1.000</p>
<p>将所得个位自上而下写出即可</p>
<p>0.625 =  0.101（二进制）</p>
</li>
</ul>
</li>
</ul>
<h3 id="二进制与八进制">二进制与八进制</h3>
<h4 id="2-8">2-&gt;8</h4>
<p>以小数点为界，分别往高、低每三位一组，不足三位补0；</p>
<h4 id="8-2">8-&gt;2</h4>
<p>将美味八进制用三位二进制数表示，小数点位置保持不变。</p>
<h3 id="2-1616-2懂得都懂~">2-&gt;16/16-&gt;2懂得都懂~</h3>
<h2 id="加减乘除">加减乘除</h2>
<h2 id="原反补">原反补</h2>
<h3 id="原码">原码</h3>
<ul>
<li>符号位用0表示正，1表示负；数值位保持不变。</li>
</ul>
<h4 id="小数原码">小数原码</h4>
<p>原个位用作符号位。</p>
<p>例如：</p>
<p>​		X1 = 0.10，原码：0.10</p>
<p>​		X2 = -0.10，原码：1.10</p>
<ul>
<li>
<p>符号位的0/1代表着“负号”的有无。</p>
<h4 id="整数原码">整数原码</h4>
</li>
</ul>
<p>在首位之前增加一位符号位</p>
<p>例如：</p>
<p>​		X1 = 101，原码：0101</p>
<p>​		X2 = -101，原码：1101</p>
<h4 id="优点-2">优点</h4>
<ul>
<li>简单易懂，求取方便。</li>
</ul>
<h4 id="缺点">缺点</h4>
<ul>
<li>加减运算不方便。</li>
<li>当两数加减运算时，要根据运算以及参加运算的两个数的符号来确定是加还是减；</li>
<li>如果做减法，首先要根据两数的大小来确定减数和被减数，以及运算结果的符号。</li>
<li>综上所述，运算较为复杂。</li>
</ul>
<h3 id="反码">反码</h3>
<ul>
<li>符号位：用0表示正，1表示负；</li>
<li>数值位：
<ul>
<li>整数的反码与原码数值位相同；</li>
<li>负数的反码是原码数值位按位取反。</li>
</ul>
</li>
</ul>
<h4 id="优点-3">优点</h4>
<ul>
<li>
<p>采用反码进行加减运算时，无论进行两数相加还是两数相减，均可通过加法实现。</p>
</li>
<li>
<p>运算时，符号位和数值位一样参加运算。当符号位产生进位时，应该将进位加到运算结果的最低位，得到运算结果。</p>
</li>
<li>
<p>[X1+X2]反 = [X1]反 + [X2]反</p>
</li>
<li>
<p>[X1-X2]反 = [X1]反 + [-X2]反 (运算时将符号位取反即可)</p>
<p>例如：</p>
<p>​		X1 = +0.1110 X2 = +0.0101</p>
<p>​		X1-X2 = 0.1110+1.1010 = 0.1001</p>
</li>
</ul>
<h3 id="补码">补码</h3>
<ul>
<li>符号位：用0表示正，1表示负；</li>
<li>数值位：
<ul>
<li>整数的反码与原码数值位相同；</li>
<li>负数的反码是原码数值位按位取反，末位+1。</li>
</ul>
</li>
</ul>
<h4 id="优点-4">优点</h4>
<ul>
<li>
<p>补码优势与反码相同。</p>
</li>
<li>
<p>运算时，符号位和数值位一样参加运算。当符号位产生进位时，应去掉进位，得到运算结果。</p>
</li>
<li>
<p>补码还原真值时，对数值位取反+1。</p>
</li>
</ul>
<h2 id="bcd码binary-coded-decimal">BCD码（Binary Coded Decimal）</h2>
<p>用四位二进制代码对十进制数字符号进行编码——二—十进制代码。</p>
<p>BCD码并不是二进制数，而是采用二进制编码的十进制数。</p>
<h3 id="8421">8421</h3>
<ul>
<li>是用4位二进制码表示一位十进制字符的一种有权码，4位二进制码从高位至低位的权依次为23、22、21、20，即为8、4、2、1,故称为8421码</li>
<li>按8421码编码的0～9与用4位二进制数表示的0～9完全一样。所以，8421码是一种人机联系时广泛使用的中间形式。</li>
</ul>
<h4 id="注意">注意</h4>
<ul>
<li>8421码中不允许出现1010～1111六种组合(因为没有十进制数字符号与其对应)。</li>
<li>十进制数字符号的8421码与<strong>相应ASCII码</strong>的低四位相同，这一特点有利于简化输入输出过程中BCD码与字符代码的转换。</li>
</ul>
<h3 id="2421">2421</h3>
<ul>
<li>
<p>2421码: 是用4位二进制码表示一位十进制字符的一种有权码，4位二进制码从高位至低位的权依次为2、4、2、1,故称为2421码。</p>
</li>
<li>
<p>若一个十进制字符X的2421码为a3 a2 a1 a0，则该字符的值为：</p>
<p>X = 2a3 + 4a2 + 2a1 + 1a0</p>
</li>
<li>
<p>2421码与十进制数之间的转换同样是按位进行的，例如，<br>
(258)10 = (0010 1011 1110)2421码<br>
(0010 0001 1110 1011)2421码 = (2185)10</p>
</li>
</ul>
<h4 id="注意-2">注意</h4>
<ul>
<li>
<p>2421码不具备单值性。例如，0101和1011都对应十进制数字5。为了与十进制字符一一对应，2421码不允许出现0101～1010的6种状态。</p>
</li>
<li>
<p>2421码是一种对9的自补代码。即一个数的2421码只要自身按位变反，便可得到该数对9的补数的2421码。</p>
<p>0100-&gt;1011 (4-&gt;5)</p>
</li>
</ul>
<h3 id="余三">余三</h3>
<ul>
<li>是由8421码加上0011形成的一种无权码，由于它的每个字符编码比相应8421码多3，故称为余3码。</li>
</ul>
<h4 id="注意-3">注意</h4>
<ul>
<li>余3码中不允许出现0000、0001、0010、1101、1110和1111六种状态。</li>
<li>余3码与十进制数进行转换时，每位十进制数字的编码都应余3。（转换之后或者之前先 &quot;-3&quot;）</li>
<li>余3码也是一种对9的自补代码。</li>
<li>两个余3码表示的十进制数字相加时，能产生正确进位信号，但对“和”必须修正。
<ul>
<li>修正的方法是：如果有进位，则结果加3；如果无进位，则结果减3。</li>
</ul>
</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[MySQL基本操作（一）]]></title>
        <id>https://caldarius.github.io/post/mysql-ji-ben-cao-zuo-yi/</id>
        <link href="https://caldarius.github.io/post/mysql-ji-ben-cao-zuo-yi/">
        </link>
        <updated>2021-05-09T09:15:03.000Z</updated>
        <content type="html"><![CDATA[<h2 id="基本操作">基本操作</h2>
<ul>
<li>所有的语句要用分号结尾。</li>
<li>discribe tablename;  --  显示表中所有的信息</li>
<li>-- 是单行注释</li>
<li>MySQL的关键字不区分大小写</li>
</ul>
<h3 id="数据库操作">数据库操作</h3>
<ul>
<li>
<p>dbe 为示例数据库</p>
</li>
<li>
<p>创建数据库</p>
<pre><code class="language-mysql">create database if not exists dbe
</code></pre>
</li>
<li>
<p>删除数据库</p>
<pre><code class="language-mysql"> drop database if not exists dbe
</code></pre>
</li>
<li>
<p>使用数据库</p>
<pre><code class="language-mysql">use `dbe` -- 如果表名或者字段名是一个特殊的字符就需要带``,关键字什么的
</code></pre>
</li>
<li>
<p>查看数据库</p>
<pre><code class="language-mysql">show westos  show databases -- 查看所有的数据库
</code></pre>
</li>
</ul>
<h3 id="数据库的列类型">数据库的列类型</h3>
<blockquote>
<p>数值</p>
</blockquote>
<table>
<thead>
<tr>
<th style="text-align:center">tinyint</th>
<th style="text-align:center">1</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center">small int</td>
<td style="text-align:center">2</td>
</tr>
<tr>
<td style="text-align:center">mediumint</td>
<td style="text-align:center">3</td>
</tr>
<tr>
<td style="text-align:center">int</td>
<td style="text-align:center">4</td>
</tr>
<tr>
<td style="text-align:center">bigint</td>
<td style="text-align:center">8</td>
</tr>
<tr>
<td style="text-align:center">float</td>
<td style="text-align:center">4</td>
</tr>
<tr>
<td style="text-align:center">double</td>
<td style="text-align:center">8</td>
</tr>
<tr>
<td style="text-align:center">decimal</td>
<td style="text-align:center">字符串浮点数，用于金融计算</td>
</tr>
</tbody>
</table>
<blockquote>
<p>字符串</p>
</blockquote>
<table>
<thead>
<tr>
<th style="text-align:center">char</th>
<th style="text-align:center">固定大小的字符串 0~255</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center">varchar</td>
<td style="text-align:center">可变字符串</td>
</tr>
<tr>
<td style="text-align:center">tinytext</td>
<td style="text-align:center">微型文本</td>
</tr>
<tr>
<td style="text-align:center">text</td>
<td style="text-align:center">文本串</td>
</tr>
</tbody>
</table>
<blockquote>
<p>时间日期</p>
</blockquote>
<p>java.until.date</p>
<table>
<thead>
<tr>
<th style="text-align:center">date</th>
<th style="text-align:center">YYYY-MM-DD</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center">time</td>
<td style="text-align:center">HH: mm: ss</td>
</tr>
<tr>
<td style="text-align:center">datetime</td>
<td style="text-align:center">YYYY-MM-DD HH: mm: ss</td>
</tr>
<tr>
<td style="text-align:center">timestamp</td>
<td style="text-align:center">时间戳 19701.1到现在的毫秒数</td>
</tr>
<tr>
<td style="text-align:center">year</td>
<td style="text-align:center">年份显示</td>
</tr>
</tbody>
</table>
<blockquote>
<p>null</p>
</blockquote>
<ul>
<li>
<p>没有值，未知</p>
</li>
<li>
<p><code>注意不能用null进行运算，其结果一定为null</code></p>
</li>
</ul>
<h3 id="数据库的字段属性">数据库的字段属性</h3>
<blockquote>
<p>unsigned</p>
</blockquote>
<ul>
<li>无符号的整数</li>
<li>声明之后该列不能为负数</li>
</ul>
<blockquote>
<p>zerofill</p>
</blockquote>
<ul>
<li>零填充</li>
<li>不足的位数，自动使用0来填充。例如：int(3),5 -- 005</li>
</ul>
<blockquote>
<p>自增</p>
</blockquote>
<ul>
<li>自动在上一条的记录上+1（默认情况）</li>
<li>通常用来设计唯一的逐渐~index，必须是整型</li>
</ul>
<blockquote>
<p>not NULL</p>
</blockquote>
<ul>
<li>字面理解</li>
</ul>
<blockquote>
<p>default</p>
</blockquote>
<ul>
<li>设置默认值，如果不指定该列的值会指定默认值 例如：sex 默认为男</li>
</ul>
<p>每个表所必需的五个字段：</p>
<ul>
<li>id 主键</li>
<li>version 乐观锁</li>
<li>is_delete 伪删除</li>
<li>gmt_create 创建时间</li>
<li>gmt_update 修改时间</li>
</ul>
<h2 id="ddl">DDL</h2>
<h3 id="创建数据库表">创建数据库表</h3>
<p><strong>注意：</strong></p>
<ul>
<li>
<p>使用英文的（），表的名称和字段尽量用 `` 包裹起来</p>
</li>
<li>
<p>comment 意为注释</p>
</li>
<li>
<p>建表时，每一行语句后面要加 &quot; , &quot;，最后一句可以省略</p>
</li>
</ul>
<pre><code class="language-mysql">	create table if not exists `user_information`(
    	`id` int not null auto_incement comment `学号`,
        `name` varchar(20) not null default `miao` comment `密码`,
        `sex` varchar(2) not null default `女` comment `性别`
        -- …………
    
    )
</code></pre>
<pre><code class="language-mysql">create table [if not exists] `table_name`(
	`字段名` 列类型 【属性】【索引】【注释】.
	…………



</code></pre>
<blockquote>
<p>查看创建数据库的语句和创建表的语句</p>
</blockquote>
<pre><code class="language-mysql">show create database/table `table/database_name`
</code></pre>
<blockquote>
<p>查看表的结构</p>
</blockquote>
<pre><code class="language-mysql">DESC database_name`
</code></pre>
<h2 id="数据表的类型">数据表的类型</h2>
<h3 id="数据库引擎">数据库引擎</h3>
<pre><code>INNODB 默认使用MYISAM 早些年使用的
</code></pre>
<table>
<thead>
<tr>
<th></th>
<th>MYISAM</th>
<th>INNODB</th>
</tr>
</thead>
<tbody>
<tr>
<td>事务支持</td>
<td>N</td>
<td>Y</td>
</tr>
<tr>
<td>数据行锁定</td>
<td>N</td>
<td>Y</td>
</tr>
<tr>
<td>外键约束</td>
<td>N</td>
<td>Y</td>
</tr>
<tr>
<td>全文索引</td>
<td>Y</td>
<td>N</td>
</tr>
<tr>
<td>表空间大小</td>
<td>较小</td>
<td>二倍</td>
</tr>
</tbody>
</table>
<p>常用规则：</p>
<ul>
<li>MYISAM节约空间，速度较快</li>
<li>INNODB安全性高，事物处理和多表多用户操作</li>
</ul>
<blockquote>
<p>在物理空间的位置</p>
</blockquote>
<p>目前未知。。。</p>
<blockquote>
<p>设置字符集编码</p>
</blockquote>
<pre><code class="language-mysql">charset=utf8 -- 写在建数据库或者建表语句括号之后
</code></pre>
<h3 id="修改和删除表">修改和删除表</h3>
<blockquote>
<p>修改表</p>
</blockquote>
<pre><code class="language-mysql">alter table tablee_name rename as new_table_name  -- 修改表名alter table tablee_name add fiel_dname datatype -- 添加字段alter table tablee_name modify field_dname datatype -- 像一次重新定义，修改约束alter table tablee_name change new_field_dname datatype -- 修改字段名（重命名）alter table tablee_name drop field_name -- 删除表的字段drop table if exists tablee_name -- 删除表 
</code></pre>
<h2 id="mysql数据管理">MySQL数据管理</h2>
<h3 id="外键">外键</h3>
<ul>
<li>数据库级别的外键不建议使用，会造成一些困扰，避免使用外键</li>
<li>数据库就是单纯的表，只用来存储数据，只有行列，行代表数据、列代表字段</li>
<li>外键用程序级别去实现</li>
<li>不得使用外键与级联，一切外键必须在应用层面解决</li>
</ul>
<h2 id="dml">DML</h2>
<ul>
<li>数据库存在的意义：数据存储，数据管理</li>
<li>DML，数据操作语言</li>
<li>insert</li>
<li>update</li>
<li>delete</li>
</ul>
<h3 id="添加insert">添加（insert）</h3>
<pre><code class="language-mysql">insert into tablename ({ field1，field2...})values(&quot;value1&quot;),(&quot;value2&quot;) # 所示是一次插入多行，对同一行的操作妨碍一个括号里面。如果不写表的字段他就会一一匹配
</code></pre>
<h3 id="修改update">修改（update）</h3>
<pre><code class="language-mysql">update tablename set colnum = value where condition
</code></pre>
<ul>
<li>colnum_name 是数据库的列，要尽量带上``，对于所有涉及到表明和列名的都建议带上</li>
<li>注意删选的条件，如果不带条件会修改所有的列</li>
<li>value，是一个具体的值，也可以是一个变量（常用于时间）</li>
<li>多个设置的属性之间使用英文的逗号隔开</li>
</ul>
<table>
<thead>
<tr>
<th>操作符</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td>=</td>
<td>==</td>
</tr>
<tr>
<td>&lt;&gt; or !=</td>
<td>!=</td>
</tr>
<tr>
<td>&gt;</td>
<td>&gt;</td>
</tr>
<tr>
<td>&lt;</td>
<td>&lt;</td>
</tr>
<tr>
<td>&lt;=</td>
<td>&lt;=</td>
</tr>
<tr>
<td>&gt;=</td>
<td>&lt;=</td>
</tr>
<tr>
<td>between...and...</td>
<td>&gt;=&amp;&amp;&lt;=</td>
</tr>
<tr>
<td>and</td>
<td>&amp;&amp;</td>
</tr>
<tr>
<td>or</td>
<td>||</td>
</tr>
</tbody>
</table>
<h3 id="删除">删除</h3>
<ul>
<li>delete</li>
</ul>
<pre><code class="language-mysql">delete from table_name where condition # 注意where，别删库跑路了
</code></pre>
<ul>
<li>
<p>truncate</p>
<p>完全亲空一个数据库表，表的结构和索引不会变</p>
<p>计数器归零，重新设置自增列，不会影响事物</p>
</li>
</ul>
<h2 id="dql">DQL</h2>
<ul>
<li>DQL 数据库查询语言</li>
</ul>
<h3 id="select">select</h3>
<pre><code class="language-mysql">select * from table_name # 查询所有数据select fieldname1,fieldname2 from table_name # 查询指定字段select fieldname1 as nickname ,fieldname2 as nickname from table_name as table_nickname # 使用别名 select concat('name:',fieldname1) from table_name # 在对应结果前面加一个字符串 name：zhangsan
</code></pre>
<ul>
<li>当列名不能见名知意的时候注意使用as</li>
</ul>
<h3 id="去重">去重</h3>
<pre><code class="language-mysql">select distinct * from tablename # 去除结果中所有的重复数据
</code></pre>
<h3 id="数据库的表达式">数据库的表达式</h3>
<pre><code class="language-mysql">select version() # 查询系统的版本select 100*3-21 as result # 用于计算select result+1 from result # 结果+1显示# 不多BB
</code></pre>
<h3 id="where">where</h3>
<ul>
<li>检索结果中符合条件的值</li>
</ul>
<h3 id="模糊查询">模糊查询</h3>
<ul>
<li>本质是一些比较运算符和通配符</li>
</ul>
<table>
<thead>
<tr>
<th>运算符</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td>is null</td>
<td>是空不？</td>
</tr>
<tr>
<td>is not null</td>
<td>非空不？</td>
</tr>
<tr>
<td>between……and……</td>
<td>不解释</td>
</tr>
<tr>
<td>like</td>
<td>用于匹配</td>
</tr>
<tr>
<td>a in （b，c，d）</td>
<td>不解释</td>
</tr>
<tr>
<td>%</td>
<td>不限个数个字符</td>
</tr>
<tr>
<td>_</td>
<td>单个字符</td>
</tr>
</tbody>
</table>
<ul>
<li>所有的非空判断要用is null or is not null，不能用“=”</li>
</ul>
<h3 id="联表查询">联表查询</h3>
<pre><code class="language-mysql">  select t1.field1,field2 from table1 as t1 inner join table2 as t2  where t1.field1 = t2.field1 -- 对于重复字段field1，用别名加以区分  select t1.field1,field2 from table1 as t1 	right join table2 as t2  where t1.field1 = t2.field1  select t1.field1,field2 from table1 as t1   left join table2 as t2  where t1.field1 = t2.field1
</code></pre>
<h3 id="自连接">自连接</h3>
<ul>
<li>自己的表和自己的表连接，核心： 一张表拆为两张一样的表即可</li>
<li>类似于用表表示树的结构，用于寻找父类或子类</li>
</ul>
<pre><code class="language-mysql">select t1.field1,t2.field2 from table as t1 , table as t2  where t1.field1 = t2.field1
</code></pre>
<h3 id="分页和排序">分页和排序</h3>
<ul>
<li>升序 ASC，降序DESC</li>
</ul>
<pre><code class="language-mysql">order by field DESCorder by field ASC
</code></pre>
<ul>
<li>分页</li>
</ul>
<pre><code class="language-mysql">limt 0,5 -- 参数分别为起始条数（从0开始）和页面大小。起始值(第n页) = (n-1)*size 
</code></pre>
<h3 id="条件书写顺序">条件书写顺序</h3>
<ul>
<li>联合查询</li>
<li>where</li>
<li>分组</li>
<li>过滤</li>
<li>排序</li>
<li>分页</li>
</ul>
<h3 id="子查询">子查询</h3>
<ul>
<li>在where语句中嵌套一个查询语句,实现类似联表查询的效果。相当于列大方程，将一个查询的结果集当作变量进行条件判断</li>
</ul>
<pre><code class="language-mysql">
</code></pre>
<h3 id="函数">函数</h3>
<ul>
<li>ABS( )</li>
<li>celing( )  上取整</li>
<li>floor( )   下取整</li>
<li>rand( )  随机数0~1</li>
<li>sign( )  返回符号 0是0，负数-1，正数1</li>
<li>char_length( ) 返回字符串长度</li>
<li>concat( a , b,c )  粘贴字符串</li>
<li>insert(s1,1,2,s2 ) 查询，替换，从某位置开始，替换某个长度</li>
<li>replace(s,s1,s2) 替换指定的字符串</li>
<li>sunstr(s1,star,size ) 截取字符串</li>
<li>current_date() 获取当前日期2021-1-30</li>
<li>now() 2020-1-30 16:40:53</li>
</ul>
<h3 id="聚合函数">聚合函数</h3>
<ul>
<li>完成各种统计及操作</li>
<li>count( * ) 也可以放个指定列，都行吧，看操作 多少条记录</li>
<li>sum( ) 求和</li>
<li>AVG( ) 平均分</li>
<li>MAX( ) 最高分</li>
<li>min(  ) 最低分</li>
</ul>
<h3 id="分组">分组</h3>
<pre><code class="language-mysql">group by field
</code></pre>
<p>分组之后where失效，用having替代where，况且where应该在group by 之前</p>
<p>不用组函数的分组是没有意义的</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[JavaSE]]></title>
        <id>https://caldarius.github.io/post/javase/</id>
        <link href="https://caldarius.github.io/post/javase/">
        </link>
        <updated>2021-04-29T07:35:48.000Z</updated>
        <content type="html"><![CDATA[<h1 id="java-se三">Java SE(三)</h1>
<h2 id="面向对象oop">面向对象(OOP)</h2>
<h3 id="面向过程pop-面向对象oop">面向过程（POP）&amp; 面向对象(OOP)</h3>
<ul>
<li>
<p>面向过程，强调的是功能行为，以函数为最小单位，考虑怎么做。</p>
</li>
<li>
<p>面向对象，将功能封装进对 象，强调具备了功能的对象，以类/对象为最小单位，考虑谁来做。</p>
</li>
<li>
<p>面向对象更贴近人类在日常的思维逻辑中采用的思想方法与原则，如 抽象、分类、继承、聚合、多态等。</p>
<ul>
<li>
<p>经典例子：</p>
<p>如何把大象装进冰箱？</p>
<ul>
<li>
<p>面向过程：<br>
{<br>
打开冰箱{</p>
<p>……</p>
<p>……</p>
<p>}</p>
<p>把大象装进冰箱 {</p>
<p>……</p>
<p>……</p>
<p>}</p>
<p>把冰箱门关住{</p>
<p>……</p>
<p>……</p>
<p>}</p>
</li>
<li>
<p>面向对象：</p>
</li>
</ul>
<p>{</p>
<p>人{</p>
<pre><code>	 打开（冰箱）{
</code></pre>
<p>冰箱.开门(); }</p>
</li>
</ul>
<p>操作(大象){<br>
大象.进入(冰箱); }<br>
关闭(冰箱){</p>
<pre><code>				冰箱.关门();
  
			} 
  
	}
  
冰箱{
  
	开门( ){ }      
</code></pre>
<p>关门( ){ }</p>
<pre><code>}
  
大象{
  
	进入(冰箱){
  
	}
</code></pre>
<p>}</p>
</li>
<li>
<p>面向对象是对面向过程的封装，屏蔽了实现的过程，保留了功能调用。</p>
</li>
</ul>
<h3 id="oop思想概述">OOP思想概述</h3>
<h4 id="三四大特征">三（四）大特征</h4>
<ul>
<li>
<p>抽象（Abstract）</p>
</li>
<li>
<p>封装 (Encapsulation)</p>
</li>
<li>
<p>继承 (Inheritance)</p>
</li>
<li>
<p>多态 (Polymorphism)</p>
</li>
</ul>
<h4 id="概述">概述</h4>
<ul>
<li>
<p>根据问题需要，选择问题所对应的实体。</p>
<ul>
<li>这个实体通常是现实世界有的，建议解决问题时向现实世界靠拢。同时，也要发挥自己的能动作用，合理构建理想事物</li>
</ul>
</li>
<li>
<p>从实体中寻找解决问题相关的属性和功能，这些属性和功能就形成了概念世界中的类。</p>
</li>
<li>
<p>把抽象的实体用计算机语言进行描述，形成计算机世界中类的定义。即借助某种程序 语言，把类构造成计算机能够识别和处理的数据结构和算法。分别对应类的属性和行为。</p>
</li>
<li>
<p>将类实例化成计算机世界中的对象。对象是我们解决问题的最终工具。</p>
<h3 id="java-的-类class-与-对象object">Java 的 类(Class) 与 对象(Object)</h3>
</li>
</ul>
<h4 id="二者的关系">二者的关系</h4>
<ul>
<li>图纸和产品的关系。</li>
<li>抽象和实体的关系。对象也成为类的实例，创建对象的过程也叫做类的实例化。</li>
<li>类的设计是面向对象程序设计的重点。</li>
</ul>
<h4 id="类的构成">类的构成</h4>
<h5 id="属性">属性</h5>
<p>对应类中的成员变量，用于描述某一类实体的属性。是构成程序的数据结构。</p>
<h5 id="行为">行为</h5>
<p>对应类中的成员方法，用于描述一类实体的行为。是构成程序的算法。</p>
<p><strong>数据结构+算法=程序</strong></p>
<h4 id="语法格式">语法格式</h4>
<pre><code class="language-java">public/protected/private class class_name{
    //属性；
    //方法；
}

// 例如：
public class Student{
    // 属性
    String Sno;
    String name;
    
    //方法
    public void hello(){
        System.out.println(&quot;Hello miao~&quot;);
    }
}
// class student 会作为后续的例子
</code></pre>
<h4 id="对象的创建和使用">对象的创建和使用</h4>
<pre><code class="language-java">class_name object_name = new class_name(); //类名 对象名 = new 类名();
object_name.object_member; // 使用  对象名.对象成员  的方式访问对象成员（属性和方法）

public class RunTime{
    public static void main(String args[]){
        Student student = new Student();
        student.Sno = &quot;201903010121&quot;;
        student.hello();
    }
}
</code></pre>
<ul>
<li>如果创建了一个类的 多个对象，对于类中 定义的属性，每个对 象都拥有各自的一套 副本，且互不干扰。后续的内存部分会介绍原理。</li>
</ul>
<h5 id="匿名对象">匿名对象</h5>
<h5 id="类的访问机制">类的访问机制</h5>
<ul>
<li>在一个类中的访问机制：类中的方法可以直接访问类中的成员变量。</li>
<li>在不同类中的访问机制：先创建要访问类的对象，再用对象访问类中 定义的成员。</li>
</ul>
<h4 id="属性-2">属性</h4>
<ul>
<li>
<p>成员变量</p>
<ul>
<li>类变量</li>
</ul>
</li>
<li>
<p>局部变量</p>
</li>
<li>
<p>初始赋值为0</p>
</li>
</ul>
<h4 id="方法-行为">方法 —— 行为</h4>
<ul>
<li>声明</li>
<li>参数列表的返回值</li>
<li>不允许嵌套</li>
<li>方法重载</li>
<li>传值机制</li>
<li>递归方法(recursion)</li>
</ul>
<h3 id="封装">封装</h3>
<h3 id="继承">继承</h3>
<h3 id="多态">多态</h3>
]]></content>
    </entry>
</feed>